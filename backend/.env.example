# Red Set ProtoCell - Backend Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env files to version control!

# =============================================================================
# APPLICATION ENVIRONMENT
# =============================================================================

# [NON-SECRET CONFIG] Environment mode: "development" or "production"
# In production, security features are enforced
RSP_ENVIRONMENT=development

# =============================================================================
# CORS CONFIGURATION
# =============================================================================

# [NON-SECRET CONFIG] Comma-separated list of allowed origins for CORS
# REQUIRED in production mode
# Example: https://app.example.com,https://dashboard.example.com
# Migration target example: https://redset.app
RSP_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# =============================================================================
# API KEYS (NEVER COMMIT THESE!)
# =============================================================================

# [NON-SECRET CONFIG] Backend Type: Choose which LLM backend to use
# Options: openai, anthropic, openrouter, llama_cpp, custom_http
# Default: openai
BACKEND_TYPE=openai

# [SECRET] OpenAI API Key (inject via Secret Manager in Cloud Run)
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=<OPENAI_API_KEY>

# [SECRET] Anthropic API Key (optional; inject via Secret Manager)
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=<ANTHROPIC_API_KEY>

# [SECRET] OpenRouter API Key (optional; inject via Secret Manager)
# Get yours at: https://openrouter.ai/keys
# OpenRouter provides access to multiple LLM providers through a unified API
OPENROUTER_API_KEY=<OPENROUTER_API_KEY>

# OpenRouter Base URL (optional, defaults to https://openrouter.ai/api/v1)
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# [SECRET] Sniper Agent API Key (optional; inject via Secret Manager)
# If not set, Sniper will operate without API access
SNIPER_ANTHROPIC_API_KEY=<SNIPER_ANTHROPIC_API_KEY>

# [SECRET] Spotter Agent API Key (optional; inject via Secret Manager)
# If not set, Spotter will operate without API access
SPOTTER_ANTHROPIC_API_KEY=<SPOTTER_ANTHROPIC_API_KEY>

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================

# [SECRET] JWT Secret Key - REQUIRED FOR PRODUCTION
# SECURITY WARNING: This MUST be set to a strong random value in production
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# NEVER commit the actual secret to version control!
RSP_JWT_SECRET=your-very-long-random-secret-key-here

# [NON-SECRET CONFIG] JWT token expiration in hours
RSP_JWT_EXPIRATION_HOURS=24

# [NON-SECRET CONFIG] Enable JWT authentication (true/false)
# SECURITY WARNING: MUST be 'true' in production
# Set to false ONLY for local development
RSP_REQUIRE_AUTH=false

# [SECRET] Demo admin password
# SECURITY WARNING: This is a demo authentication system for development only
# The password is hashed using PBKDF2 but users are stored in memory
# IN PRODUCTION: Implement proper user management with a database
# REQUIRED: Set this to a strong password - it must not be left empty
# Example: RSP_DEMO_PASSWORD=your_secure_random_password_here
RSP_DEMO_PASSWORD=

# [SECRET] API Keys for programmatic access (optional)
# Format: key1:role1,key2:role2
# Roles: admin, researcher, observer
# Example: abc123:admin,def456:researcher
# SECURITY WARNING: Store actual API keys in a secure secrets manager
RSP_API_KEYS=

# =============================================================================
# RATE LIMITING
# =============================================================================

# [NON-SECRET CONFIG] Maximum requests per minute per IP address
RSP_RATE_LIMIT_PER_MIN=60

# [NON-SECRET CONFIG] Maximum requests per hour per IP address
RSP_RATE_LIMIT_PER_HOUR=1000

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# [NON-SECRET CONFIG, CURRENTLY UNWIRED IN RUNTIME] SQLite database path
RSP_DB_PATH=rsp_session.db

# [SECRET, FUTURE PHASE] PostgreSQL connection string for Cloud SQL cutover
# Example: postgresql://user:password@localhost:5432/rsp
RSP_POSTGRES_URI=
# SQLAlchemy database URL used for early-access persistence
RSP_DATABASE_URL=postgresql://<user>:<password>@localhost/redset_protocell
# SMTP notification settings for early-access signup alerts
RSP_SMTP_HOST=smtp.gmail.com
RSP_SMTP_PORT=587
RSP_SMTP_USER=your-email@gmail.com
RSP_SMTP_PASSWORD=your-app-password
RSP_ADMIN_EMAIL=admin@redset.app

# =============================================================================
# LOGGING
# =============================================================================

# [NON-SECRET CONFIG, CURRENTLY UNWIRED IN RUNTIME] Log level: DEBUG, INFO, WARNING, ERROR
RSP_LOG_LEVEL=INFO

# [NON-SECRET CONFIG, CURRENTLY UNWIRED IN RUNTIME] Log file path (optional, defaults to stdout)
RSP_LOG_FILE=

# =============================================================================
# WEBSOCKET CONFIGURATION
# =============================================================================

# [NON-SECRET CONFIG, CURRENTLY UNWIRED IN RUNTIME] Maximum number of concurrent WebSocket connections
RSP_MAX_WEBSOCKET_CONNECTIONS=100

# =============================================================================
# SESSION STORAGE
# =============================================================================

# [NON-SECRET CONFIG, CURRENTLY UNWIRED IN RUNTIME] Directory for session data
RSP_SESSIONS_DIR=sessions

# =============================================================================
# MONITORING & OBSERVABILITY (Optional)
# =============================================================================

# [SECRET, CURRENTLY UNWIRED IN RUNTIME] Sentry DSN for error tracking (optional)
# Get yours at: https://sentry.io/
SENTRY_DSN=

# [SECRET, CURRENTLY UNWIRED IN RUNTIME] Rollbar access token (optional)
# Get yours at: https://rollbar.com/
ROLLBAR_TOKEN=

# =============================================================================
# NOTES
# =============================================================================

# For production deployment (migration target):
# 1. Set RSP_ENVIRONMENT=production
# 2. Set RSP_ALLOWED_ORIGINS to your frontend domain (e.g., https://redset.app)
# 3. Generate a strong RSP_JWT_SECRET (min 32 characters)
# 4. Change RSP_DEMO_PASSWORD to a strong password (or implement proper user management)
# 5. Set RSP_REQUIRE_AUTH=true
# 6. Configure monitoring tools (Sentry, etc.)
# 7. Review and adjust rate limits based on your needs
# 8. Future phase: move to Cloud SQL PostgreSQL (do not cut over in current phase)
# 9. Set up log aggregation and monitoring
# 10. Configure automated backups

# Security best practices:
# - NEVER commit this file with real secrets
# - Use environment-specific .env files (.env.production, .env.development)
# - Rotate secrets regularly (at least quarterly)
# - Use a secrets manager in production (AWS Secrets Manager, HashiCorp Vault, etc.)
# - Restrict file permissions: chmod 600 .env
# - Enable HTTPS/TLS for all production endpoints
# - Implement rate limiting and DDoS protection
# - Regularly audit access logs
# - Use database connection pooling
# - Implement proper user session management with a database
# - Enable API request logging for security audits
# - Consider implementing 2FA for admin accounts

# Authentication system notes:
# - Current system uses PBKDF2 password hashing (100k iterations)
# - Users are stored in memory (not persistent across restarts)
# - FOR PRODUCTION: Implement database-backed user management
# - FOR PRODUCTION: Consider using OAuth2/OIDC providers
# - FOR PRODUCTION: Implement password complexity requirements
# - FOR PRODUCTION: Add account lockout after failed login attempts
# - FOR PRODUCTION: Log all authentication events for security monitoring
