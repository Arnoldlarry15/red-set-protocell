# Red Set ProtoCell - Python Dependencies

# Core dependencies
python-dateutil>=2.8.2
numpy>=1.20.0,<2.0.0

# Async support
aiosqlite>=0.19.0

# API clients - REQUIRED (no mock backends)
openai>=1.0.0
anthropic>=0.7.0

# Optional: Local model support (GGUF via llama-cpp-python)
# Uncomment to enable local GGUF model support:
# llama-cpp-python>=0.2.0

# Optional: Custom HTTP backend support
requests>=2.31.0

# Web server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0  # Production WSGI server
websockets>=12.0
pydantic>=2.0.0

# Security and authentication
PyJWT>=2.8.0  # JWT token generation and validation
